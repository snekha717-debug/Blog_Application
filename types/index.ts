export interface Post {
  id: string; // MockAPI uses strings for IDs
  title: string;
  body: string;
  thumbnail: string;
  authorName: string;
  authorAvatar: string;
  createdAt: string;
}